php artisan tinker
Dentro do tinker, cola:

use App\Models\User;
use Illuminate\Support\Facades\Hash;

User::updateOrCreate(
  ['email' => 'admin@angomoni.test'],
  [
    'name' => 'Administrador AngoMoni',
    'password' => Hash::make('Admin@12345'),
    'papel' => 'ADMIN',
    'ativo' => true,
  ]
);
Confirma:

User::where('papel','ADMIN')->count();